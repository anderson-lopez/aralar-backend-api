services:
  api:
    build: .
    env_file: .env
    depends_on: [mongo]
    ports: ["8000:8000"]
    volumes: [".:/app"]
  mongo:
    image: mongo:7
    ports: ["27017:27017"]
    volumes:
      - mongo_data:/data/db
volumes:
  mongo_data:
